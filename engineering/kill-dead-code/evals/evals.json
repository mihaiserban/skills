{
  "skill_name": "kill-dead-code",
  "evals": [
    {
      "id": 1,
      "name": "remove-unused-function",
      "prompt": "Clean up this module. I think some of these functions are never called. The only things used from this file are `calculateTotal` and `formatCurrency` — grep the codebase and remove anything unreachable.\n\n```typescript\n// pricing.ts\nexport function calculateTotal(items: Array<{price: number, quantity: number}>): number {\n  return items.reduce((sum, item) => sum + item.price * item.quantity, 0);\n}\n\nexport function formatCurrency(amount: number): string {\n  return `$${amount.toFixed(2)}`;\n}\n\nexport function applyDiscount(price: number, percent: number): number {\n  return price * (1 - percent / 100);\n}\n\nexport function roundUp(price: number): number {\n  return Math.ceil(price * 100) / 100;\n}\n\nexport function getTaxRate(region: string): number {\n  const rates: Record<string, number> = {\n    'US': 0.08,\n    'EU': 0.20,\n    'JP': 0.10\n  };\n  return rates[region] ?? 0;\n}\n\nexport function calculateTax(price: number, region: string): number {\n  return price * getTaxRate(region);\n}\n```\n\nTell me what's dead and should be removed.",
      "files": [],
      "assertions": [
        {"id": "identifies-dead", "text": "Identifies applyDiscount, roundUp, getTaxRate, and calculateTax as dead code", "type": "quality"},
        {"id": "keeps-live", "text": "Keeps calculateTotal and formatCurrency since they're the used exports", "type": "quality"},
        {"id": "explains-why", "text": "Explains why each removed function is dead (no references found)", "type": "format"},
        {"id": "warns-exports", "text": "Warns that unused exports might be public API — suggests checking before deleting", "type": "behavior"}
      ]
    },
    {
      "id": 2,
      "name": "remove-orphaned-imports",
      "prompt": "I removed `formatDate` from `utils.ts`. Check the imports across the codebase and clean up anything that's now orphaned.\n\n```typescript\n// Current state of utils.ts after removal:\nexport function slugify(text: string): string {\n  return text.toLowerCase().replace(/\\s+/g, '-');\n}\n\nexport function truncate(text: string, maxLen: number): string {\n  return text.length > maxLen ? text.slice(0, maxLen) + '...' : text;\n}\n```\n\n```typescript\n// Some other file that still imports formatDate:\nimport { formatDate, slugify } from './utils';\n```",
      "files": [],
      "assertions": [
        {"id": "flags-orphan-import", "text": "Flags that formatDate is imported but no longer exists in utils.ts", "type": "quality"},
        {"id": "cleanup-instruction", "text": "Tells the user to remove formatDate from the import and any callers", "type": "behavior"},
        {"id": "slugify-kept", "text": "Keeps the slugify import since it still exists", "type": "quality"}
      ]
    }
  ]
}
